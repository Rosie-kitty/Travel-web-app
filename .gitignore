# ─────────────────────────────────────────────────────────────────────────────
#                              Общие правила
# ─────────────────────────────────────────────────────────────────────────────

# Игнорировать файл с локальными переменными окружения (например, общий .env)
.env
.env.local

# Игнорировать все node_modules для API
services/api/node_modules/

# Игнорировать папку с собранными файлами (build/dist) для API
services/api/dist/
services/api/build/

# Игнорировать логи и временные файлы
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Игнорировать локальные IDE- и операционные файлы
.vscode/
.idea/
*.suo
*.user
*.cache
*.db
*.DS_Store
Thumbs.db

# ─────────────────────────────────────────────────────────────────────────────
#                               Файлы .env для сервисов
# ─────────────────────────────────────────────────────────────────────────────

# API / NestJS
services/api/.env

# AI / FastAPI
services/ai/.env

# ─────────────────────────────────────────────────────────────────────────────
#                               Docker-артефакты
# ─────────────────────────────────────────────────────────────────────────────

# Если у вас есть локальные файлы настроек Docker (push, credentials и т.п.)
**/docker-compose.override.yml
**/docker-compose.*.env

# ─────────────────────────────────────────────────────────────────────────────
#                             Прочие зависимости
# ─────────────────────────────────────────────────────────────────────────────

# Python cache (если вдруг вы в AI используете __pycache__)
**/__pycache__/
**/*.py[cod]
**/*.pyo

# Например, если AI собирает виртуальное окружение внутри папки ai/venv:
services/ai/venv/

# ─────────────────────────────────────────────────────────────────────────────
#                   Игнорировать всё остальное, что локально не нужно
# ─────────────────────────────────────────────────────────────────────────────

# Любые другие генераторы/сборщики, логи, кэши и временные файлы
*.tmp
*.temp
